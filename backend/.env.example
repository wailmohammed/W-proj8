# Example environment variables for backend
CURRENT_PROVIDER=yfinance
# Optional: connection string for Redis, e.g. redis://redis:6379/0
REDIS_URL=
# Provider API keys (if switching providers)
FMP_KEY=
